<?xml version="1.0" encoding="UTF-8"?><sqlb_project><db path="netflix.db" readonly="0" foreign_keys="0" case_sensitive_like="0" temp_store="0" wal_autocheckpoint="1000" synchronous="2"/><attached/><window><main_tabs open="structure browser pragmas query" current="3"/></window><tab_structure><column_width id="0" width="300"/><column_width id="1" width="0"/><column_width id="2" width="100"/><column_width id="3" width="1494"/><column_width id="4" width="0"/><expanded_item id="0" parent="1"/><expanded_item id="1" parent="1"/><expanded_item id="2" parent="1"/><expanded_item id="3" parent="1"/></tab_structure><tab_browse><table title="netflix_titles" custom_title="0" dock_id="2" table="4,14:mainnetflix_titles"/><dock_state state=""/><default_encoding codec=""/><browse_table_settings><table schema="main" name="netflix_titles" show_row_id="0" encoding="" plot_x_axis="" unlock_view_pk="_rowid_" freeze_columns="0"><sort/><column_widths><column index="1" value="63"/><column index="2" value="59"/><column index="3" value="300"/><column index="4" value="300"/><column index="5" value="300"/><column index="6" value="300"/><column index="7" value="124"/><column index="8" value="92"/><column index="9" value="51"/><column index="10" value="73"/><column index="11" value="300"/><column index="12" value="300"/></column_widths><filter_values/><conditional_formats/><row_id_formats/><display_formats/><hidden_columns/><plot_y_axes/><global_filter/></table></browse_table_settings></tab_browse><tab_sql><sql name="SQL 1*">-- 👉 Cuenta cuántos registros (filas) hay en la tabla netflix_titles.
-- Es lo primero que se suele hacer para ver el tamaño de la base de datos.
SELECT 
    COUNT(*) as total_registros    -- COUNT(*) cuenta todas las filas de la tabla; AS total_registros le pone un nombre al resultado
FROM 
    netflix_titles;                -- FROM indica la tabla sobre la que se ejecuta el conteo
</sql><sql name="SQL 2*">-- 👉 Agrupa el contenido por tipo (Movie o TV Show) y cuenta cuántos hay de cada uno.
-- Sirve para ver la proporción de películas vs series.
SELECT 
    type,                          -- Selecciona la columna 'type' (por ejemplo 'Movie' o 'TV Show')
    COUNT(*) as cantidad           -- Cuenta las filas de cada grupo (por tipo) y le pone el alias 'cantidad'
FROM 
    netflix_titles                 -- Tabla origen de los datos
GROUP BY 
    type;                          -- GROUP BY agrupa las filas por el valor de 'type' para calcular COUNT por cada grupo
</sql><sql name="SQL 3*">-- 👉 Agrupa por año de lanzamiento (release_year), cuenta cuántos títulos se lanzaron ese año,
-- los ordena de mayor a menor y muestra los 10 años con más producciones.
SELECT 
    release_year,                  -- Selecciona la columna 'release_year' (año de lanzamiento)
    COUNT(*) as cantidad           -- Cuenta la cantidad de títulos por cada año
FROM 
    netflix_titles                 -- Tabla origen
WHERE 
    release_year IS NOT NULL       -- Excluye filas sin año (NULL) para evitar conteos erráticos
GROUP BY 
    release_year                   -- Agrupa por año para obtener un conteo por cada año
ORDER BY 
    cantidad DESC                  -- Ordena los años por la cantidad de títulos, de mayor a menor
LIMIT 
    10;                            -- Limita la salida a los 10 primeros resultados (top 10 años)
</sql><sql name="SQL 4*">-- 👉 Análisis de contenido por década y tipo, calculando además % de contenido adulto.
WITH contenido_por_decada AS (         -- Define el primer CTE llamado 'contenido_por_decada'
    SELECT 
        CASE 
            WHEN release_year &gt;= 2020 THEN '2020s'    -- Si el año &gt;= 2020, etiqueta '2020s'
            WHEN release_year &gt;= 2010 THEN '2010s'    -- Si el año está entre 2010 y 2019, etiqueta '2010s'
            WHEN release_year &gt;= 2000 THEN '2000s'    -- Si el año está entre 2000 y 2009, etiqueta '2000s'
            WHEN release_year &gt;= 1990 THEN '1990s'    -- Si el año está entre 1990 y 1999, etiqueta '1990s'
            ELSE 'VIEJA'                             -- Si es anterior o no encaja, etiqueta 'VIEJA'
        END as decada,                               -- Alias de la columna calculada: 'decada'
        type,                                        -- Incluye la columna 'type' (Movie/TV Show)
        COUNT(*) as cantidad,                        -- Cuenta títulos por combinación (decada, type)
        AVG(                                         -- AVG de una expresión 1/0 devuelve la proporción de 1s
            CASE WHEN rating IN ('TV-MA', 'R') THEN 1 ELSE 0 END
        ) * 100 as porcentaje_adulto                -- Multiplica por 100 para obtener porcentaje de contenido adulto
    FROM 
        netflix_titles                              -- Fuente de los datos
    WHERE 
        release_year IS NOT NULL                    -- Excluye registros sin año
    GROUP BY 
        decada, type                                -- Agrupa por década y tipo para calcular COUNT y AVG por categoría
), 
estadisticas_generales AS (                      -- Define el segundo CTE con estadísticas agregadas
    SELECT 
        AVG(cantidad) as promedio_por_categoria    -- Calcula el promedio de 'cantidad' entre todas las filas de contenido_por_decada
    FROM 
        contenido_por_decada                       -- Usa el CTE anterior como fuente
)
SELECT 
    cpd.decada,                                   -- Selecciona la década del CTE principal (alias cpd)
    cpd.type,                                     -- Selecciona el tipo (Movie/TV Show)
    cpd.cantidad,                                 -- Número de títulos en esa combinación decada+type
    ROUND(cpd.porcentaje_adulto, 1) as porcentaje_contenido_adulto,  -- Redondea el porcentaje a 1 decimal
    CASE 
        WHEN cpd.cantidad &gt; eg.promedio_por_categoria THEN 'Por encima del promedio'  -- Compara con el promedio general
        ELSE 'Por debajo del promedio'
    END as clasificacion                          -- Devuelve una etiqueta que clasifica la categoría respecto al promedio
FROM 
    contenido_por_decada cpd                      -- Fuente principal: filas por década+tipo
CROSS JOIN 
    estadisticas_generales eg                     -- CROSS JOIN replica la única fila de estadísticas generales para comparar en cada fila
ORDER BY 
    cpd.decada DESC,                              -- Ordena por década descendente (más recientes primero)
    cpd.cantidad DESC;                            -- Dentro de cada década ordena por cantidad descendente
</sql><sql name="SQL 5*">-- 👉 Ranking anual de países productores (solo países únicos, desde 2015),
-- mostrando producción del año, ranking dentro del año, producción año anterior y crecimiento.
SELECT 
    country,                                      -- País productor (columna 'country')
    release_year,                                 -- Año de lanzamiento
    COUNT(*) as producciones_año,                  -- Conteo de producciones de ese país en ese año
    RANK() OVER (PARTITION BY release_year         -- RANK calcula el puesto dentro de cada partición (aquí, cada año)
        ORDER BY COUNT(*) DESC                     -- Ordena por la cantidad de producciones (descendente) para asignar el ranking
    ) as ranking_anual,                            -- Alias: ranking anual del país para ese año
    LAG(COUNT(*)) OVER (PARTITION BY country       -- LAG trae el valor del grupo previo según el ORDER BY dentro de la partición
        ORDER BY release_year                      -- Para cada country, LAG mira el año anterior (orden temporal)
    ) as producciones_año_anterior,                -- Alias: producciones del mismo país en el año anterior
    COUNT(*) - LAG(COUNT(*)) OVER (PARTITION BY country
        ORDER BY release_year
    ) as crecimiento                               -- Diferencia entre producciones del año y del año anterior
FROM 
    netflix_titles                                 -- Fuente de datos
WHERE 
    country IS NOT NULL                            -- Excluye filas sin país
    AND country NOT LIKE '%,%'                     -- Excluye registros con comas (evita coproducciones listadas como &quot;X, Y&quot;)
    AND release_year &gt;= 2015                       -- Considera solo años desde 2015 en adelante
GROUP BY 
    country, release_year                          -- Agrupa por país y año para obtener los conteos por combinación
HAVING 
    COUNT(*) &gt;= 3                                  -- Filtra: solo mantiene grupos con al menos 3 producciones por año
ORDER BY 
    release_year DESC,                             -- Ordena por año (más reciente primero)
    ranking_anual;                                 -- Dentro de cada año ordena por el ranking anual calculado
</sql><sql name="SQL 6*">-- 👉 Simulación de un ROLLUP (detalle + subtotales + totales) usando UNION ALL y luego ordenando.
WITH resumen AS (                                  -- CTE que agrupa todos los bloques del UNION para ordenarlos después
    -- Bloque 1: detalle por tipo, rating y periodo
    SELECT 
        type as tipo_contenido,                    -- Columna 'type' renombrada a 'tipo_contenido' para el reporte
        rating as clasificacion,                   -- Columna 'rating' renombrada a 'clasificacion'
        CASE 
            WHEN release_year &gt;= 2020 THEN '2020+' -- Define el periodo según el año
            WHEN release_year &gt;= 2010 THEN '2010-2019'
            ELSE 'Antes 2010'
        END as periodo,                            -- Alias 'periodo' con el rango temporal
        COUNT(*) as cantidad_titulos,              -- Conteo de títulos para esta combinación (tipo, rating, periodo)
        ROUND(                                     
            COUNT(*) * 100.0 / (                  -- Calcula porcentaje sobre el total filtrado por release_year IS NOT NULL y rating IS NOT NULL
                SELECT COUNT(*) 
                FROM netflix_titles 
                WHERE release_year IS NOT NULL 
                  AND rating IS NOT NULL
            ), 2
        ) as porcentaje_total                      -- Porcentaje redondeado con 2 decimales
    FROM 
        netflix_titles
    WHERE 
        release_year IS NOT NULL 
        AND rating IS NOT NULL                     -- Excluye filas sin año o sin rating para consistencia del denominador
    GROUP BY 
        type, rating,                              -- Agrupa por tipo y rating
        CASE 
            WHEN release_year &gt;= 2020 THEN '2020+'
            WHEN release_year &gt;= 2010 THEN '2010-2019'
            ELSE 'Antes 2010'
        END

    UNION ALL

    -- Bloque 2: subtotales por tipo y rating (sin dividir por periodo)
    SELECT 
        type as tipo_contenido,                    -- Mismo alias para mantener columnas consistentes en el UNION
        rating as clasificacion,                   
        'TOTAL' as periodo,                         -- 'TOTAL' indica que este registro es un subtotal sobre periodos
        COUNT(*) as cantidad_titulos,              
        ROUND(
            COUNT(*) * 100.0 / (
                SELECT COUNT(*) 
                FROM netflix_titles 
                WHERE release_year IS NOT NULL 
                  AND rating IS NOT NULL
            ), 2
        ) as porcentaje_total
    FROM 
        netflix_titles
    WHERE 
        release_year IS NOT NULL 
        AND rating IS NOT NULL
    GROUP BY 
        type, rating                               -- Agrupa solo por tipo y rating para obtener subtotales

    UNION ALL

    -- Bloque 3: subtotales por tipo solamente (sin clasificacion ni periodo)
    SELECT 
        type as tipo_contenido,
        'TOTAL' as clasificacion,                   -- 'TOTAL' en clasificacion para indicar subtotal por tipo
        'TOTAL' as periodo,                         -- 'TOTAL' en periodo
        COUNT(*) as cantidad_titulos,
        ROUND(
            COUNT(*) * 100.0 / (
                SELECT COUNT(*) 
                FROM netflix_titles 
                WHERE release_year IS NOT NULL 
                  AND rating IS NOT NULL
            ), 2
        ) as porcentaje_total
    FROM 
        netflix_titles
    WHERE 
        release_year IS NOT NULL 
        AND rating IS NOT NULL
    GROUP BY 
        type                                        -- Agrupa por tipo para obtener subtotales por tipo

    UNION ALL

    -- Bloque 4: total general
    SELECT 
        'TOTAL' as tipo_contenido,                  -- Marca que es el total general
        'TOTAL' as clasificacion,                   
        'TOTAL' as periodo,                         
        COUNT(*) as cantidad_titulos,               -- Conteo total de todos los registros (filtrados)
        100.0 as porcentaje_total                   -- El porcentaje del total sobre sí mismo es 100%
    FROM 
        netflix_titles
    WHERE 
        release_year IS NOT NULL 
        AND rating IS NOT NULL
)
SELECT 
    *                                             -- Selecciona todas las columnas del CTE 'resumen'
FROM 
    resumen
ORDER BY 
    CASE WHEN tipo_contenido = 'TOTAL' THEN 2 ELSE 1 END,  -- Empuja las filas marcadas 'TOTAL' al final del grupo de tipo
    tipo_contenido,                                      -- Ordena por tipo de contenido (alfabéticamente)
    CASE WHEN clasificacion = 'TOTAL' THEN 2 ELSE 1 END,  -- Empuja subtotales de clasificación al final dentro de cada tipo
    clasificacion,                                       -- Orden por clasificación (rating)
    CASE WHEN periodo = 'TOTAL' THEN 2 ELSE 1 END,       -- Empuja registros de periodo 'TOTAL' al final dentro de cada subgrupo
    periodo;                                             -- Orden final por periodo
</sql><current_tab id="5"/></tab_sql></sqlb_project>
